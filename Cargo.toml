[package]
name = "baby-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
opencv = { version = "*", default-features = false, features = ["imgcodecs", "videoio"]}
tokio = { version = "1.0", features = ["full"] }
